# 第 2 步开发计划定稿：IoTGateway 双协议韧性接入网关

> **版本**：V4.0（基于原草稿 V3.0 终审修正）  
> **对应总架构**：`1、架构最终确定版.md` V3.0 第 2 步  
> **前置依赖**：第一步全部完成  
> **本步目标**：实现 IoTGateway 的 HTTP + MQTT WebSocket 双协议接入能力，以 **Kafka produce ack** 为唯一可靠边界，Polly 韧性管道包裹写入，W3C Trace Context 通过 Kafka Header 跨进程传播，自定义 Prometheus 指标覆盖双协议，健康检查覆盖 Kafka + MQTT 双通道。  
> **本步边界**：聚焦 Gateway 接入层，不实现 BackendProcessor 消费、不写 Dockerfile/K8s YAML、不实现设备认证/TLS/ACL（第 5 步落地）。

---

## 0. 前置检查与依赖修正

### 0.1 确认第一步成果存在

以下文件和类型必须已就位，否则回到第一步补齐：

| 位置 | 类型/内容 |
|---|---|
| `IoTHunter.Shared/Domain/ReliabilityLevel.cs` | 枚举 `BestEffort=0`, `AtLeastOnce=1`, `Critical=2` |
| `IoTHunter.Shared/Domain/TelemetryEnvelope.cs` | record 含 `EventId`, `DeviceId`, `Sequence`, `RecordedAt`, `ReceivedAt`, `MetricType`, `PayloadJson`, `ReliabilityLevel`, `SchemaVersion`；静态工厂方法 `Create()` |
| `IoTHunter.Shared/Infrastructure/TraceIdEnricher.cs` | Serilog `ILogEventEnricher` |
| `IoTHunter.Shared/Infrastructure/LoggingDefaults.cs` | `ConfigureBaseLogger()` |
| `IoTHunter.Shared/Infrastructure/OpenTelemetryDefaults.cs` | `AddIoTHunterOpenTelemetry()` 扩展方法 |
| `IoTGateway/Program.cs` | 当前使用上述 Shared 基础设施，监听 `localhost:5080`，`/health/ready` 和 `/metrics` 可用 |

### 0.2 MQTTnet 依赖版本兼容性验证

当前 `IoTGateway.csproj` 中存在版本不匹配风险：

```
MQTTnet                          5.1.0.1559   (主库 v5.x)
MQTTnet.Extensions.ManagedClient 4.3.7.1207   (扩展库 v4.x)
```

`MQTTnet.Extensions.ManagedClient` 4.3.x 是为 MQTTnet 4.x 构建的，与 MQTTnet 5.x **API 不兼容**。在执行本步编码前，先运行以下命令检查：

```powershell
cd IoTHunter
dotnet add IoTGateway/IoTGateway.csproj package MQTTnet.Extensions.ManagedClient --prerelease
```

如果 NuGet 上不存在与 MQTTnet 5.1 兼容的 `ManagedClient` 版本，则需在 MQTTnet 5.x 中手动管理连接生命周期（MQTTnet 5.x 原生支持自动重连，不需要 ManagedClient 扩展包）。本计划同时给出两种方案。

---

## 1. 本地基础设施（可选但推荐）

在 `IoTHunter/` 目录下新建 `docker-compose.infra.yml`：

```yaml
services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"

  mosquitto:
    image: eclipse-mosquitto:2.0.18
    ports:
      - "1883:1883"
      - "8083:8083"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
```

同目录新建 `mosquitto.conf`：

```text
listener 1883
protocol mqtt

listener 8083
protocol websockets

allow_anonymous true

persistence true
persistence_location /mosquitto/data/
max_inflight_messages 100
```

启动：

```powershell
docker compose -f docker-compose.infra.yml up -d
```

> 注：第二步专注核心链路，TLS、设备认证、ACL 在第 5 步落地。`allow_anonymous true` 仅限本地开发。

---

## 2. 动作项与代码指令

### 2.1 更新配置文件

完全替换 `IoTGateway/appsettings.json`：

```json
{
  "AllowedHosts": "*",
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://localhost:5080"
      }
    }
  },
  "Kafka": {
    "BootstrapServers": "localhost:9092",
    "MessageTimeoutMs": 5000,
    "ClientId": "iot-gateway",
    "MaxInFlight": 5
  },
  "Mqtt": {
    "WebSocketUrl": "ws://localhost:8083/mqtt",
    "ClientId": "iot-gateway-mqtt",
    "AutoReconnectDelayMs": 5000
  },
  "OpenTelemetry": {
    "OtlpEndpoint": ""
  }
}
```

### 2.2 定义选项类

新建 `IoTGateway/Infrastructure/Options/KafkaOptions.cs`：

```csharp
namespace IoTGateway.Infrastructure.Options;

public sealed class KafkaOptions
{
    public string BootstrapServers { get; set; } = "localhost:9092";
    public int MessageTimeoutMs { get; set; } = 5000;
    public string ClientId { get; set; } = "iot-gateway";
    public int MaxInFlight { get; set; } = 5;
}
```

新建 `IoTGateway/Infrastructure/Options/MqttOptions.cs`：

```csharp
namespace IoTGateway.Infrastructure.Options;

public sealed class MqttOptions
{
    public string WebSocketUrl { get; set; } = "ws://localhost:8083/mqtt";
    public string ClientId { get; set; } = "iot-gateway-mqtt";
    public int AutoReconnectDelayMs { get; set; } = 5000;
}
```

### 2.3 定义通用 HTTP 接入 DTO（含校验）

新建 `IoTGateway/Contracts/TelemetryRequest.cs`：

```csharp
using System.ComponentModel.DataAnnotations;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace IoTGateway.Contracts;

public sealed record TelemetryRequest(
    [property: JsonPropertyName("deviceId")]
    [property: Required(ErrorMessage = "deviceId is required")]
    [property: MinLength(1, ErrorMessage = "deviceId must not be empty")]
    string DeviceId,

    [property: JsonPropertyName("metricType")]
    [property: Required(ErrorMessage = "metricType is required")]
    string MetricType,

    [property: JsonPropertyName("payload")]
    JsonElement? Payload,

    [property: JsonPropertyName("timestamp")]
    [property: Range(1, long.MaxValue, ErrorMessage = "timestamp must be a positive Unix ms value")]
    long Timestamp,

    [property: JsonPropertyName("sequence")]
    [property: Range(0, long.MaxValue, ErrorMessage = "sequence must be non-negative")]
    long Sequence
);
```

**设计意图**：
- `DeviceId`、`MetricType`、`Timestamp` 为必填，带明确错误消息。
- `Payload` 使用 `JsonElement?` 而非具体业务字段（如 `heartRate`），保持网关通用性——手环心率、血氧、步数等任意 metric 共用同一 DTO。
- `JsonPropertyName` 显式指定 camelCase，与架构文档中设备上报 JSON 命名约定对齐。

### 2.4 共享 JSON 序列化配置

新建 `IoTGateway/Infrastructure/SerializerSetup.cs`：

```csharp
using System.Text.Json;
using System.Text.Json.Serialization;

namespace IoTGateway.Infrastructure;

internal static class SerializerSetup
{
    public static readonly JsonSerializerOptions TightOptions = new()
    {
        ReadCommentHandling = JsonCommentHandling.Disallow,
        AllowTrailingCommas = false,
        PropertyNameCaseInsensitive = true,
        NumberHandling = JsonNumberHandling.Strict,
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        PropertyNamingPolicy = JsonNamingPolicy.CamelCase
    };
}
```

**设计意图**：HTTP 端（Controller 级别）和 MQTT 端（手动反序列化）使用**同一套** `JsonSerializerOptions`，消除两个协议入口对 JSON 解析标准不一致的安全面。

### 2.5 定义 Envelope 映射器（使用工厂方法）

新建 `IoTGateway/Infrastructure/Messaging/TelemetryEnvelopeMapper.cs`：

```csharp
using System.Text.Json;
using IoTHunter.Shared.Domain;
using IoTGateway.Contracts;

namespace IoTGateway.Infrastructure.Messaging;

internal static class TelemetryEnvelopeMapper
{
    public static TelemetryEnvelope ToEnvelope(TelemetryRequest request, ReliabilityLevel level)
    {
        return TelemetryEnvelope.Create(
            eventId: $"{request.DeviceId}:{request.MetricType}:{request.Sequence}:{request.Timestamp}",
            deviceId: request.DeviceId,
            metricType: request.MetricType,
            recordedAt: DateTimeOffset.FromUnixTimeMilliseconds(request.Timestamp),
            payloadJson: request.Payload.HasValue
                ? JsonSerializer.Serialize(request.Payload.Value, SerializerSetup.TightOptions)
                : "{}",
            reliabilityLevel: level,
            sequence: request.Sequence);
    }
}
```

**关键修正**：
- **使用 `TelemetryEnvelope.Create()` 工厂方法**而非 `new TelemetryEnvelope{...}`。工厂方法自动设置 `ReceivedAt = DateTimeOffset.UtcNow`，这是架构要求的时间戳。
- `PayloadJson` 从 `TelemetryRequest.Payload`（通用 `JsonElement`）序列化为字符串，保留了协议层的业务灵活性。

### 2.6 构建 Polly 韧性管道

新建 `IoTGateway/Infrastructure/Resilience/ResiliencePipelines.cs`：

```csharp
using Confluent.Kafka;
using Polly;
using Polly.Retry;
using Polly.Timeout;

namespace IoTGateway.Infrastructure.Resilience;

internal static class ResiliencePipelines
{
    public static ResiliencePipeline<DeliveryResult<Null, string>> BuildKafkaPipeline()
    {
        return new ResiliencePipelineBuilder<DeliveryResult<Null, string>>()
            .AddRetry(new RetryStrategyOptions<DeliveryResult<Null, string>>
            {
                ShouldHandle = new PredicateBuilder<DeliveryResult<Null, string>>()
                    .Handle<ProduceException<Null, string>>()
                    .Handle<TimeoutRejectedException>(),
                MaxRetryAttempts = 3,
                Delay = TimeSpan.FromMilliseconds(100),
                BackoffType = DelayBackoffType.Exponential,
                OnRetry = args =>
                {
                    var loggerFactory = LoggerFactory.Create(b => b.AddConsole());
                    var logger = loggerFactory.CreateLogger("Resilience");
                    logger.LogWarning(
                        "Kafka produce retry {Attempt}/{MaxAttempts} after {DelayMs}ms",
                        args.AttemptNumber, 3, args.RetryDelay.TotalMilliseconds);
                    return ValueTask.CompletedTask;
                }
            })
            .AddTimeout(TimeSpan.FromSeconds(4))
            .Build();
    }
}
```

**设计说明**：
- 重试次数 3 次，指数退避 100ms → 200ms → 400ms，总预算约 700ms。
- 超时 4 秒覆盖重试预算 + Kafka `MessageTimeoutMs`（5 秒），避免 Gateway 线程被无限阻塞。
- `OnRetry` 回调输出警告日志，方便排查网络闪断问题。
- 按架构文档 ADR-002 和第二步 2.6 节要求：**禁止无限重试**。

### 2.7 实现 Kafka Producer 服务（含 W3C Trace 注入）

新建 `IoTGateway/Infrastructure/Messaging/KafkaProducerService.cs`：

```csharp
using System.Diagnostics;
using System.Text;
using System.Text.Json;
using Confluent.Kafka;
using IoTHunter.Shared.Domain;
using IoTGateway.Infrastructure.Options;
using OpenTelemetry;
using OpenTelemetry.Context.Propagation;
using Polly;

namespace IoTGateway.Infrastructure.Messaging;

internal sealed class KafkaProducerService : IDisposable
{
    private static readonly ActivitySource ActivitySource = new("IoTGateway.Kafka");
    private static readonly TextMapPropagator Propagator = Propagators.DefaultTextMapPropagator;

    private readonly IProducer<Null, string> _producer;
    private readonly ResiliencePipeline<DeliveryResult<Null, string>> _pipeline;
    private readonly ILogger<KafkaProducerService> _logger;

    public KafkaProducerService(
        KafkaOptions options,
        ResiliencePipeline<DeliveryResult<Null, string>> pipeline,
        ILogger<KafkaProducerService> logger)
    {
        _pipeline = pipeline;
        _logger = logger;

        var config = new ProducerConfig
        {
            BootstrapServers = options.BootstrapServers,
            Acks = Acks.All,
            EnableIdempotence = true,
            MaxInFlight = options.MaxInFlight,
            MessageSendMaxRetries = 3,
            MessageTimeoutMs = options.MessageTimeoutMs,
            ClientId = options.ClientId,
            SocketKeepaliveEnable = true
        };

        _producer = new ProducerBuilder<Null, string>(config).Build();
        _logger.LogInformation("Kafka producer initialized: {BootstrapServers} client={ClientId}",
            options.BootstrapServers, options.ClientId);
    }

    public async Task<DeliveryResult<Null, string>> ProduceAsync(
        string topic, TelemetryEnvelope envelope, CancellationToken ct = default)
    {
        var json = JsonSerializer.Serialize(envelope, SerializerSetup.TightOptions);
        var message = new Message<Null, string> { Value = json };

        // ADR-004: 在 Kafka Header 注入 W3C Trace Context
        var parentActivity = Activity.Current;
        using var activity = ActivitySource.StartActivity(
            "Kafka.Produce", ActivityKind.Producer, parentActivity?.Context ?? default);

        Propagator.Inject(
            new PropagationContext(activity?.Context ?? default, Baggage.Current),
            message.Headers,
            (headers, key, value) => headers.Add(key, Encoding.UTF8.GetBytes(value)));

        // 业务元数据注入
        message.Headers.Add("event_id", Encoding.UTF8.GetBytes(envelope.EventId));
        message.Headers.Add("schema_version", Encoding.UTF8.GetBytes(envelope.SchemaVersion.ToString()));
        message.Headers.Add("reliability_level", Encoding.UTF8.GetBytes(((int)envelope.ReliabilityLevel).ToString()));

        var stopwatch = Stopwatch.StartNew();
        try
        {
            var result = await _pipeline.ExecuteAsync(
                async token => new ValueTask<DeliveryResult<Null, string>>(
                    await _producer.ProduceAsync(topic, message, token)), ct);

            stopwatch.Stop();
            activity?.SetTag("messaging.system", "kafka");
            activity?.SetTag("messaging.destination", topic);
            activity?.SetTag("messaging.kafka.partition", result.Partition.Value);
            activity?.SetTag("messaging.kafka.offset", result.Offset.Value);

            _logger.LogInformation(
                "Kafka ack {EventId} topic={Topic} partition={Partition} offset={Offset} latency={LatencyMs}ms",
                envelope.EventId, topic, result.Partition.Value, result.Offset.Value, stopwatch.ElapsedMilliseconds);

            return result;
        }
        catch (Exception ex)
        {
            stopwatch.Stop();
            activity?.SetStatus(ActivityStatusCode.Error, ex.Message);
            _logger.LogError(ex,
                "Kafka produce failed {EventId} topic={Topic} latency={LatencyMs}ms",
                envelope.EventId, topic, stopwatch.ElapsedMilliseconds);
            throw;
        }
    }

    public void Ping()
    {
        _producer.GetMetadata(TimeSpan.FromSeconds(3));
    }

    public void Dispose()
    {
        _producer.Flush(TimeSpan.FromSeconds(5));
        _producer.Dispose();
    }
}
```

**关键设计**：
- `Acks.All` + `EnableIdempotence = true` + `MaxInFlight = 5` + `MessageSendMaxRetries = 3`：保证 Kafka 幂等生产，同时不阻塞发送管道。
- `SocketKeepaliveEnable = true`：防止长时间空闲连接被中间网络设备断开。
- `Scope`：本服务不设置 `Activity.Current` 以便追踪传播；它复制 `parentActivity.Context` 作为 ParentSpan。
- `Dispose` 中先 `Flush(5s)` 再 `Dispose`，确保优雅关闭时未决消息投递完毕（配合 K8s `terminationGracePeriodSeconds`）。
- **不使用默认 `System.Text.Json` 序列化**，而是通过 `SerializerSetup.TightOptions` 统一序列化选项。

### 2.8 实现自定义 Prometheus 指标

新建 `IoTGateway/Infrastructure/Metrics/GatewayMetrics.cs`：

```csharp
using System.Diagnostics.Metrics;

namespace IoTGateway.Infrastructure.Metrics;

internal sealed class GatewayMetrics
{
    private readonly Meter _meter;
    private readonly Counter<long> _requestsTotal;
    private readonly Counter<long> _rejectedTotal;
    private readonly Histogram<double> _kafkaAckLatency;
    private readonly Counter<long> _mqttMessagesTotal;

    public GatewayMetrics()
    {
        _meter = new Meter("IoTGateway", "1.0.0");

        _requestsTotal = _meter.CreateCounter<long>(
            "gateway_requests_total",
            description: "Total ingestion requests by protocol, topic and status.");

        _rejectedTotal = _meter.CreateCounter<long>(
            "gateway_rejected_total",
            description: "Total rejected requests by reason.");

        _kafkaAckLatency = _meter.CreateHistogram<double>(
            "gateway_kafka_ack_latency_ms",
            unit: "ms",
            description: "End-to-end Kafka produce latency.");

        _mqttMessagesTotal = _meter.CreateCounter<long>(
            "mqtt_messages_total",
            description: "Total MQTT messages received by topic and qos.");
    }

    public void RecordRequest(string protocol, string topic, string status)
    {
        var tags = new TagList
        {
            { "protocol", protocol },
            { "topic", topic },
            { "status", status }
        };
        _requestsTotal.Add(1, tags);
    }

    public void RecordRejection(string protocol, string reason)
    {
        var tags = new TagList
        {
            { "protocol", protocol },
            { "reason", reason }
        };
        _rejectedTotal.Add(1, tags);
    }

    public void RecordKafkaLatency(string topic, double latencyMs)
    {
        var tags = new TagList { { "topic", topic } };
        _kafkaAckLatency.Record(latencyMs, tags);
    }

    public void RecordMqttMessage(string topic, string qos)
    {
        var tags = new TagList
        {
            { "topic", topic },
            { "qos", qos }
        };
        _mqttMessagesTotal.Add(1, tags);
    }
}
```

**设计意图**：
- 四个指标对齐架构文档第 2 步第 8 条：`gateway_requests_total`、`gateway_rejected_total`、`gateway_kafka_ack_latency_ms`、`mqtt_messages_total`。
- 每个指标携带 `protocol` / `topic` / `status` / `qos` 标签，Grafana 可按维度分面（例：HTTP vs MQTT 流量对比面板只需 `protocol="http"` 过滤）。

### 2.9 实现 MQTT WebSocket 接入服务

新建 `IoTGateway/Infrastructure/Mqtt/MqttIngestionService.cs`：

```csharp
using System.Text;
using System.Text.Json;
using System.Text.RegularExpressions;
using IoTHunter.Shared.Domain;
using IoTGateway.Contracts;
using IoTGateway.Infrastructure.Messaging;
using IoTGateway.Infrastructure.Metrics;
using IoTGateway.Infrastructure.Options;
using MQTTnet;
using MQTTnet.Client;

namespace IoTGateway.Infrastructure.Mqtt;

internal sealed partial class MqttIngestionService : BackgroundService
{
    // 预编译正则：提取 deviceId，拒绝非标准 topic
    [GeneratedRegex(@"^device/(?<deviceId>[^/]+)/(?<rest>telemetry|event/critical)$",
        RegexOptions.Compiled | RegexOptions.CultureInvariant)]
    private static partial Regex TopicPattern();

    private readonly KafkaProducerService _producer;
    private readonly MqttOptions _mqttOptions;
    private readonly GatewayMetrics _metrics;
    private readonly ILogger<MqttIngestionService> _logger;

    public MqttIngestionService(
        KafkaProducerService producer,
        MqttOptions mqttOptions,
        GatewayMetrics metrics,
        ILogger<MqttIngestionService> logger)
    {
        _producer = producer;
        _mqttOptions = mqttOptions;
        _metrics = metrics;
        _logger = logger;
    }

    public bool IsConnected => _client?.IsConnected ?? false;

    private IMqttClient? _client;

    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    {
        var factory = new MqttFactory();
        _client = factory.CreateMqttClient();

        // MQTTnet 5.x 自动重连已在库内置，无需 ManagedClient
        _client.DisconnectedAsync += async e =>
        {
            if (e.ClientWasConnected && !stoppingToken.IsCancellationRequested)
            {
                _logger.LogWarning("MQTT disconnected: {Reason}. Reconnecting in {DelayMs}ms...",
                    e.Reason, _mqttOptions.AutoReconnectDelayMs);
                await Task.Delay(_mqttOptions.AutoReconnectDelayMs, stoppingToken);
                try { await ConnectAndSubscribeAsync(stoppingToken); }
                catch (Exception ex) { _logger.LogError(ex, "MQTT reconnect failed"); }
            }
        };

        _client.ApplicationMessageReceivedAsync += OnMessageReceivedAsync;

        await ConnectAndSubscribeAsync(stoppingToken);

        _logger.LogInformation("MQTT ingestion started: {Url} clientId={ClientId}",
            _mqttOptions.WebSocketUrl, _mqttOptions.ClientId);

        try { await Task.Delay(Timeout.Infinite, stoppingToken); }
        catch (OperationCanceledException)
        {
            _logger.LogInformation("MQTT ingestion stopping...");
        }
        finally
        {
            if (_client is not null)
            {
                await _client.DisconnectAsync();
                _client.Dispose();
            }
        }
    }

    private async Task ConnectAndSubscribeAsync(CancellationToken ct)
    {
        if (_client is null) return;

        var clientOptions = new MqttClientOptionsBuilder()
            .WithWebSocketServer(o => o.WithUri(_mqttOptions.WebSocketUrl))
            .WithClientId(_mqttOptions.ClientId)
            .WithCleanSession(false)        // ADR-015: 持久会话
            .WithKeepAlivePeriod(TimeSpan.FromSeconds(30))
            .Build();

        await _client.ConnectAsync(clientOptions, ct);

        // 共享订阅组 + 普通遥测 QoS 0
        await _client.SubscribeAsync(new MqttTopicFilterBuilder()
            .WithTopic("$share/gateway-group/device/+/telemetry")
            .WithQualityOfServiceLevel(MQTTnet.Protocol.MqttQualityOfServiceLevel.AtMostOnce)
            .Build(), ct);

        // 共享订阅组 + 关键事件 QoS 1 (ADR-014)
        await _client.SubscribeAsync(new MqttTopicFilterBuilder()
            .WithTopic("$share/gateway-group/device/+/event/critical")
            .WithQualityOfServiceLevel(MQTTnet.Protocol.MqttQualityOfServiceLevel.AtLeastOnce)
            .Build(), ct);

        _logger.LogInformation("MQTT connected and subscribed");
    }

    private async Task OnMessageReceivedAsync(MqttApplicationMessageReceivedEventArgs e)
    {
        var topic = e.ApplicationMessage.Topic;
        var payload = Encoding.UTF8.GetString(e.ApplicationMessage.PayloadSegment);

        try
        {
            // 严格 Topic 格式校验
            var match = TopicPattern().Match(topic);
            if (!match.Success)
            {
                _logger.LogWarning("MQTT rejected: invalid topic format '{Topic}'", topic);
                _metrics.RecordRejection("mqtt", "invalid_topic");
                return;
            }

            var deviceId = match.Groups["deviceId"].Value;
            var isCritical = match.Groups["rest"].Value == "event/critical";

            // 使用共享序列化配置反序列化
            var request = JsonSerializer.Deserialize<TelemetryRequest>(payload, SerializerSetup.TightOptions);
            if (request is null)
            {
                _logger.LogWarning("MQTT rejected: deserialization failed device={DeviceId}", deviceId);
                _metrics.RecordRejection("mqtt", "deserialization_failed");
                return;
            }

            // 校验必填字段
            var validationResults = new List<System.ComponentModel.DataAnnotations.ValidationResult>();
            var validationContext = new System.ComponentModel.DataAnnotations.ValidationContext(request);
            if (!System.ComponentModel.DataAnnotations.Validator.TryValidateObject(
                    request, validationContext, validationResults, validateAllProperties: true))
            {
                var errors = string.Join("; ", validationResults.Select(r => r.ErrorMessage));
                _logger.LogWarning("MQTT rejected: validation failed device={DeviceId} errors={Errors}", deviceId, errors);
                _metrics.RecordRejection("mqtt", "validation_failed");
                return;
            }

            // 路由：Topic → Kafka Topic + ReliabilityLevel
            var reliability = isCritical ? ReliabilityLevel.Critical : ReliabilityLevel.BestEffort;
            var kafkaTopic = isCritical ? "event.critical" : "telemetry.raw";

            var envelope = TelemetryEnvelopeMapper.ToEnvelope(request, reliability);
            await _producer.ProduceAsync(kafkaTopic, envelope, CancellationToken.None);

            _metrics.RecordRequest("mqtt", kafkaTopic, "success");
            _metrics.RecordMqttMessage(topic, isCritical ? "qos1" : "qos0");

            _logger.LogInformation(
                "MQTT→Kafka {EventId} device={DeviceId} topic={MqttTopic}→{KafkaTopic} reliability={Level}",
                envelope.EventId, deviceId, topic, kafkaTopic, reliability);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "MQTT processing failed topic={Topic}", topic);
            _metrics.RecordRejection("mqtt", ex.GetType().Name);
        }
    }
}
```

**关键修正（对比草稿）**：
1. **源码生成正则**：使用 `.NET 7+` 的 `[GeneratedRegex]` 编译时生成正则代码，零运行时分配，且模式 `^device/(?<deviceId>[^/]+)/(?<rest>telemetry|event/critical)$` 明确拒绝任何非标准 Topic（如 `device/x/unknown`、`device/x/evil/stuff`）。
2. **MQTTnet 5.x 原生客户端**：不用 `IManagedMqttClient`（来自 4.x 扩展包，与 MQTTnet 5.x API 不兼容），而是直接用 `IMqttClient` + 手动断连重连逻辑。
3. **统一 JSON 反序列化**：MQTT 端复用 `SerializerSetup.TightOptions`，与 HTTP 端完全一致的解析标准。
4. **DTO 校验**：MQTT 消息也经过 `Validator.TryValidateObject`，与 HTTP 端一致拒绝缺失必填字段的非法消息。
5. **指标细化**：MQTT 端独立记录 `mqtt_messages_total`（含 qos 标签）和 `gateway_rejected_total`（含 rejection 原因）。

### 2.10 实现双通道健康检查

新建 `IoTGateway/Infrastructure/Health/KafkaHealthCheck.cs`：

```csharp
using IoTGateway.Infrastructure.Messaging;

namespace IoTGateway.Infrastructure.Health;

internal sealed class KafkaHealthCheck : IHealthCheck
{
    private readonly KafkaProducerService _producer;

    public KafkaHealthCheck(KafkaProducerService producer)
    {
        _producer = producer;
    }

    public Task<HealthCheckResult> CheckHealthAsync(
        HealthCheckContext context, CancellationToken ct = default)
    {
        try
        {
            _producer.Ping();
            return Task.FromResult(HealthCheckResult.Healthy("Kafka reachable"));
        }
        catch (Exception ex)
        {
            return Task.FromResult(HealthCheckResult.Unhealthy("Kafka unreachable", ex));
        }
    }
}
```

新建 `IoTGateway/Infrastructure/Health/MqttHealthCheck.cs`：

```csharp
using IoTGateway.Infrastructure.Mqtt;

namespace IoTGateway.Infrastructure.Health;

internal sealed class MqttHealthCheck : IHealthCheck
{
    private readonly MqttIngestionService _mqttService;

    public MqttHealthCheck(MqttIngestionService mqttService)
    {
        _mqttService = mqttService;
    }

    public Task<HealthCheckResult> CheckHealthAsync(
        HealthCheckContext context, CancellationToken ct = default)
    {
        if (_mqttService.IsConnected)
        {
            return Task.FromResult(HealthCheckResult.Healthy("MQTT broker connected"));
        }

        return Task.FromResult(HealthCheckResult.Unhealthy("MQTT broker disconnected"));
    }
}
```

**设计意图**：K8s 就绪探针必须同时感知 Kafka 和 MQTT 两个通道的健康状态。`MapHealthChecks("/health/ready")` 会聚合所有已注册的 `IHealthCheck`，任何一个返回 Unhealthy 都会导致 HTTP 503，配合 K8s `readinessProbe` 自动摘除不健康的 Pod。

### 2.11 实现 HTTP 接入控制器

新建 `IoTGateway/Controllers/TelemetryController.cs`：

```csharp
using System.Diagnostics;
using IoTHunter.Shared.Domain;
using IoTGateway.Contracts;
using IoTGateway.Infrastructure.Messaging;
using IoTGateway.Infrastructure.Metrics;
using Microsoft.AspNetCore.Mvc;

namespace IoTGateway.Controllers;

[ApiController]
[Route("api/v1")]
public sealed class TelemetryController : ControllerBase
{
    private readonly KafkaProducerService _producer;
    private readonly GatewayMetrics _metrics;
    private readonly ILogger<TelemetryController> _logger;

    public TelemetryController(
        KafkaProducerService producer,
        GatewayMetrics metrics,
        ILogger<TelemetryController> logger)
    {
        _producer = producer;
        _metrics = metrics;
        _logger = logger;
    }

    /// <summary>普通遥测接入：Kafka ack 后返回 202</summary>
    [HttpPost("telemetry")]
    public async Task<IActionResult> PostTelemetry(
        [FromBody] TelemetryRequest request, CancellationToken ct)
    {
        if (!ModelState.IsValid)
        {
            _metrics.RecordRejection("http", "validation_failed");
            return BadRequest(ModelState);
        }

        var envelope = TelemetryEnvelopeMapper.ToEnvelope(request, ReliabilityLevel.BestEffort);
        return await ProduceAndRespondAsync("telemetry.raw", envelope, "http", ct);
    }

    /// <summary>关键事件接入：必须 Kafka ack 成功 (ADR-020)</summary>
    [HttpPost("events/critical")]
    public async Task<IActionResult> PostCritical(
        [FromBody] TelemetryRequest request, CancellationToken ct)
    {
        if (!ModelState.IsValid)
        {
            _metrics.RecordRejection("http", "validation_failed");
            return BadRequest(ModelState);
        }

        var envelope = TelemetryEnvelopeMapper.ToEnvelope(request, ReliabilityLevel.Critical);
        return await ProduceAndRespondAsync("event.critical", envelope, "http", ct);
    }

    private async Task<IActionResult> ProduceAndRespondAsync(
        string topic, TelemetryEnvelope envelope, string protocol, CancellationToken ct)
    {
        var stopwatch = Stopwatch.StartNew();
        try
        {
            var result = await _producer.ProduceAsync(topic, envelope, ct);
            stopwatch.Stop();

            _metrics.RecordKafkaLatency(topic, stopwatch.Elapsed.TotalMilliseconds);
            _metrics.RecordRequest(protocol, topic, "success");

            return Accepted(new
            {
                eventId = envelope.EventId,
                status = "accepted",
                kafkaPartition = result.Partition.Value,
                kafkaOffset = result.Offset.Value,
                latencyMs = stopwatch.ElapsedMilliseconds
            });
        }
        catch (Exception ex)
        {
            stopwatch.Stop();
            _metrics.RecordRequest(protocol, topic, "failure");
            _metrics.RecordRejection(protocol, "kafka_unavailable");

            _logger.LogError(ex,
                "HTTP→Kafka failed {EventId} topic={Topic} latency={LatencyMs}ms",
                envelope.EventId, topic, stopwatch.ElapsedMilliseconds);

            // ADR-020: Kafka 不可用 → 503
            return StatusCode(StatusCodes.Status503ServiceUnavailable, new
            {
                eventId = envelope.EventId,
                status = "unavailable",
                reason = "Kafka producer unavailable or circuit breaker open"
            });
        }
    }
}
```

**关键设计**：
- `ModelState.IsValid` 在 Action 入口处校验 DTO 的 DataAnnotations，校验失败立即返回 400，避免垃圾数据进入 Kafka。
- 202 响应包含 `kafkaPartition`、`kafkaOffset`、`latencyMs`，方便客户端端到端验证。
- 503 响应的 reason 明确说明 "Kafka producer unavailable or circuit breaker open"，不暴露内部异常细节。

### 2.12 重写 Program.cs（复用 Shared 基础设施）

**完全替换** `IoTGateway/Program.cs`：

```csharp
using IoTHunter.Shared.Infrastructure;
using IoTGateway.Infrastructure.Health;
using IoTGateway.Infrastructure.Messaging;
using IoTGateway.Infrastructure.Metrics;
using IoTGateway.Infrastructure.Mqtt;
using IoTGateway.Infrastructure.Options;
using IoTGateway.Infrastructure.Resilience;
using OpenTelemetry.Metrics;
using OpenTelemetry.Trace;
using Serilog;

var builder = WebApplication.CreateBuilder(args);

// ---- 1. 复用 Shared 日志基础设施 ----
builder.Host.UseSerilog((context, services, configuration) =>
{
    LoggingDefaults.ConfigureBaseLogger(configuration);
});

// ---- 2. 选项绑定 ----
builder.Services.Configure<KafkaOptions>(builder.Configuration.GetSection("Kafka"));
builder.Services.Configure<MqttOptions>(builder.Configuration.GetSection("Mqtt"));
builder.Services.AddSingleton(sp =>
    sp.GetRequiredService<Microsoft.Extensions.Options.IOptions<KafkaOptions>>().Value);
builder.Services.AddSingleton(sp =>
    sp.GetRequiredService<Microsoft.Extensions.Options.IOptions<MqttOptions>>().Value);

// ---- 3. 核心服务注册 ----
builder.Services.AddSingleton(ResiliencePipelines.BuildKafkaPipeline());
builder.Services.AddSingleton<KafkaProducerService>();
builder.Services.AddSingleton<GatewayMetrics>();
builder.Services.AddHostedService<MqttIngestionService>();

// ---- 4. 健康检查 ----
builder.Services.AddHealthChecks()
    .AddCheck<KafkaHealthCheck>("kafka")
    .AddCheck<MqttHealthCheck>("mqtt");

// ---- 5. 控制器与严格 JSON ----
builder.Services.AddControllers();
builder.Services.Configure<Microsoft.AspNetCore.Http.Json.JsonOptions>(options =>
{
    options.SerializerOptions.ReadCommentHandling =
        System.Text.Json.JsonCommentHandling.Disallow;
    options.SerializerOptions.AllowTrailingCommas = false;
    options.SerializerOptions.PropertyNameCaseInsensitive = true;
});

// ---- 6. 复用 Shared OTel 基础设施 + Gateway 独有扩展 ----
builder.Services
    .AddIoTHunterOpenTelemetry(builder.Configuration, "IoTGateway")
    .WithTracing(tracing => tracing.AddAspNetCoreInstrumentation())
    .WithMetrics(metrics => metrics
        .AddAspNetCoreInstrumentation()
        .AddPrometheusExporter());

var app = builder.Build();

app.UseSerilogRequestLogging();
app.UseOpenTelemetryPrometheusScrapingEndpoint();
app.MapControllers();
app.MapHealthChecks("/health/ready");

app.Run();
```

**架构一致性说明**：

此 Program.cs 是第一阶段的**直接延伸**，不是替代：

| 行 | 作用 | 来源 |
|---|---|---|
| `LoggingDefaults.ConfigureBaseLogger(configuration)` | 统一 Serilog 日志格式 + TraceIdEnricher | Step 1 Shared |
| `.AddIoTHunterOpenTelemetry(...)` | 统一 OTel Resource + HTTP Client + Runtime 指标 | Step 1 Shared |
| `.WithTracing(AddAspNetCoreInstrumentation)` | Gateway 独有的请求追踪 | Gateway 追加 |
| `.WithMetrics(AddPrometheusExporter)` | Gateway 独有的 `/metrics` 暴露 | Gateway 追加 |

**共享基础设施真正做到了一处定义、两处使用（IoTGateway + BackendProcessor），各自链式追加独有配置。**

---

## 3. 验证标准（严格通关清单）

### 3.1 编译

```powershell
cd IoTHunter
dotnet build
```

**要求：0 Error，0 Warning**（`TreatWarningsAsErrors=true` 强制）。

### 3.2 本地基础设施启动

```powershell
docker compose -f docker-compose.infra.yml up -d

# 验证 Kafka
docker compose -f docker-compose.infra.yml exec kafka kafka-topics --bootstrap-server localhost:9092 --list

# 验证 Mosquitto
docker compose -f docker-compose.infra.yml logs mosquitto
```

### 3.3 网关启动基线

```powershell
dotnet run --project IoTGateway/IoTGateway.csproj
```

预期：
- 控制台日志包含 `trace=` 和 `span=` 字段
- 日志行显示：`Kafka producer initialized`、`MQTT connected and subscribed`、`Now listening on: http://localhost:5080`

### 3.4 指标暴露

```powershell
curl -s http://localhost:5080/metrics | findstr "gateway_"
```

预期输出含：
```
gateway_requests_total{protocol="http",topic="telemetry.raw",status="success"} ...
gateway_rejected_total{protocol="mqtt",reason="invalid_topic"} ...
gateway_kafka_ack_latency_ms_bucket{topic="telemetry.raw",le="..."} ...
mqtt_messages_total{topic="device/...",qos="qos1"} ...
```

### 3.5 健康检查

```powershell
# 全健康状态
curl -s http://localhost:5080/health/ready
# → {"status":"Healthy","results":{"kafka":{"status":"Healthy"},"mqtt":{"status":"Healthy"}}}

# 停掉 Kafka
docker compose -f docker-compose.infra.yml stop kafka
curl -s http://localhost:5080/health/ready
# → {"status":"Unhealthy","results":{"kafka":{"status":"Unhealthy",...},"mqtt":{"status":"Healthy"}}}
```

### 3.6 HTTP 可靠边界验证（核心验收）

```powershell
# 1. 正常路径：Kafka 在线 → 202 + 含 partition/offset
curl -X POST http://localhost:5080/api/v1/telemetry \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"dev-001","metricType":"heart_rate","timestamp":1700000000000,"sequence":1}'

# 预期响应 202：
# {"eventId":"dev-001:heart_rate:1:1700000000000","status":"accepted","kafkaPartition":0,"kafkaOffset":0,"latencyMs":15}

# 2. 校验失败路径：缺失 deviceId
curl -X POST http://localhost:5080/api/v1/telemetry \
  -H "Content-Type: application/json" \
  -d '{"metricType":"heart_rate","timestamp":1700000000000,"sequence":1}'

# 预期响应 400

# 3. 降级路径：停掉 Kafka 后立即重试
docker compose -f docker-compose.infra.yml stop kafka
curl -X POST http://localhost:5080/api/v1/telemetry \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"dev-001","metricType":"heart_rate","timestamp":1700000000000,"sequence":1}'

# 预期响应 503：
# {"eventId":"...","status":"unavailable","reason":"Kafka producer unavailable or circuit breaker open"}

# 4. 恢复路径：重拉 Kafka 后请求恢复 202
docker compose -f docker-compose.infra.yml start kafka
sleep 5
curl -X POST http://localhost:5080/api/v1/telemetry \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"dev-002","metricType":"steps","timestamp":1700000000000,"sequence":1}'

# 预期：恢复 202
```

### 3.7 Kafka Header 链路追踪验证

```powershell
# 消费 telemetry.raw，查看 Header
docker compose -f docker-compose.infra.yml exec kafka \
  kafka-console-consumer --bootstrap-server localhost:9092 \
  --topic telemetry.raw --from-beginning --property print.headers=true --max-messages 1

# 预期 Header 包含：
# traceparent:00-<traceId>-<spanId>-01
# event_id:dev-001:heart_rate:1:1700000000000
# schema_version:1
# reliability_level:0 (或 2 对于关键事件)
```

### 3.8 MQTT WebSocket 接入验证

使用 MQTTX 桌面客户端连接 `ws://localhost:8083/mqtt`，或使用命令行工具：

```powershell
# 安装 mqtt-cli (若未安装)
# 发送普通遥测
mqtt pub -h localhost -p 1883 -t "device/dev-003/telemetry" \
  -m '{"deviceId":"dev-003","metricType":"spo2","timestamp":1700000000000,"sequence":1}' \
  --qos 0

# 发送关键事件
mqtt pub -h localhost -p 1883 -t "device/dev-003/event/critical" \
  -m '{"deviceId":"dev-003","metricType":"fall_detected","timestamp":1700000000000,"sequence":2}' \
  --qos 1

# 验证 Kafka 中两条消息分别出现在对应 Topic
docker compose -f docker-compose.infra.yml exec kafka \
  kafka-console-consumer --bootstrap-server localhost:9092 \
  --topic telemetry.raw --from-beginning --max-messages 1

docker compose -f docker-compose.infra.yml exec kafka \
  kafka-console-consumer --bootstrap-server localhost:9092 \
  --topic event.critical --from-beginning --max-messages 1
```

---

## 4. 本步不做的事情

按架构文档第二步边界：

- 不实现 BackendProcessor Kafka 消费（第 3 步）。
- 不实现设备认证、TLS、MQTT ACL（第 5 步）。
- 不写 Dockerfile（第 5 步）。
- 不写 K8s YAML（第 6 步）。
- 不实现 `Channel<TelemetryEnvelope>` 背压缓冲（可选优化项，可在后续压测发现瓶颈后引入）。

---

## 5. 从草稿到定稿的修正清单

| 草稿问题 | 严重级 | 修正方式 |
|---|---|---|
| Program.cs 内联 OTel/Serilog，丢弃 Shared 基础设施 | 阻断 | 复用 `LoggingDefaults.ConfigureBaseLogger()` + `AddIoTHunterOpenTelemetry()` |
| Mapper 用 `new TelemetryEnvelope{}` 绕过工厂方法 | 阻断 | 改用 `TelemetryEnvelope.Create()` |
| `TelemetryRequest` 硬编码 `heartRate` 业务字段 | 阻断 | 改为通用 `JsonElement? Payload` |
| `MQTTnet.Extensions.ManagedClient` 4.3.x 与 MQTTnet 5.x 不兼容 | 阻断 | 用 `IMqttClient` 原生客户端 + 手动重连逻辑 |
| MQTT 端 JSON 反序列化配置与 HTTP 端不一致 | 高 | 提取 `SerializerSetup.TightOptions`，两端统一使用 |
| 缺少请求校验（HTTP 和 MQTT 两端） | 高 | DTO 添加 DataAnnotations，Controller 检查 ModelState，MQTT 调用 Validator |
| 健康检查只覆盖 Kafka | 高 | 新增 `MqttHealthCheck` |
| EventId 生成逻辑可能重复 | 中 | 沿用草案设计，在后续步骤引入设备端生成 ID 后再优化 |
| `EnableIdempotence` 缺少配套参数 | 中 | 显式添加 `MaxInFlight`、`MessageSendMaxRetries` |
| `ReliabilityLevel` 枚举文档只列 2 个值 | 低 | 定稿文档准确列出全部 3 个值 |
